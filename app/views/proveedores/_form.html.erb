<%= simple_form_for(@proveedore, :html => { :class => 'form-horizontal', remote: true } ) do |f| %>
  <% if @proveedore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proveedore.errors.count, "error") %> prohibited this proveedore from being saved:</h2>

      <ul>
      <% @proveedore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :telefono %><br>
    <%= f.text_field :telefono %>
  </div>
  <div class="field">
    <%= f.label :direccion %><br>
    <%= f.text_field :direccion %>
  </div>
  <div class="field">
    <%= f.label :correo %><br>
    <%= f.text_field :correo %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar", class: "btn btn-success" %>
  </div>
<% end %>
