<h1>Imágenes</h1>
<div class="conim">

    <% @images.each do |image| %>
    <div class="coni">
     <div class="conit">
        <div class="btn-group">
          <a href="#"> </a>
          <ul class="list-inline">
           <li class="link"><%= link_to '<i class="fa fa-wrench txt-primary"></i><span> Editar</span>'.html_safe, edit_tela_image_path(@tela,image) %></li>
           <li><%= link_to '<i class="fa fa-times txt-danger"></i><span> Eliminar</span>'.html_safe, tela_image_path(@tela,image), method: :delete, data: { confirm: 'Estas segur@?' } %></li>
          </ul>
        </div>
       <hr class="hr1"></div> <div class="ow-server">
       <%=link_to image_tag(image.imagen.url(:medium), height: '250', width: '250'), tela_image_path(@tela,image) %>
      </div>
      </div>
    <% end %>
</div>
<br>

<%= button_to 'Nueva Imagen', new_tela_image_path(@tela), class: "btn btn-success", :method => "get" %>
