<div class="separa">
  <center><h3>Clasificaciones</h3></center>
  <div class="divsof">
  <table class="table table-hover table-heading">
    <thead>
      <tr>
        <th class="cen">Nombre</th>
        <th class="cen">Descripción</th>
      </tr>
    </thead>

    <tbody>
    <% @clasificaciones.each do |clasificacione| %>
      <tr>
          <td><%= clasificacione.nombre %></td>
          <td><%= link_to '<button class="btn btn-default"><small> Info </small><i class="fa fa-eye"></i></button>'.html_safe, clasificacione, remote: true %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
  </div>
</div>