<%= simple_form_for(@tipos_tela, :html => { :class => 'form-horizontal', remote: true } ) do |f| %>
  <% if @tipos_tela.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipos_tela.errors.count, "error") %> prohibited this tipos_tela from being saved:</h2>

      <ul>
      <% @tipos_tela.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar" , class: "btn btn-success"%>
  </div>
<% end %>
