<%= simple_form_for(@bases_tela, :html => { :class => 'form-horizontal', remote: true } ) do |f| %>
  <% if @bases_tela.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bases_tela.errors.count, "error") %> prohibited this bases_tela from being saved:</h2>

      <ul>
      <% @bases_tela.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   <label>Clasificación</label><br>
    <%= f.collection_select :clasificacione_id, Clasificacione.all, :id, :nombre, class: "form-control", prompt: "Seleccione clasificación" %>
  </div>
  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar", class: "btn btn-success" %>
  </div>
<% end %>
