<tr>
        <td><%= uso.nombre.capitalize %></td>
        <% if current_user.role_id == 1 %>
        <td>
          <div class="btn-group">
            <a href="#"> </a>
            <ul class="list-inline">
              <li> <%= link_to '<button class="btn btn-primary"> <i class="fa fa-wrench neg"></i> Editar</button>'.html_safe,edit_uso_path(uso), remote: true %></li>
              <li> <%= link_to '<button class="btn btn-danger"><i class="fa fa-times neg"></i> Eliminar</button>'.html_safe, uso, method: :delete, data: { confirm: 'Estas segur@?' }, remote: true%></li>
            </ul>
          </div>  
        </td>
        <% end %>
      </tr>