<%= form_for(@uso) do |f| %>
  <% if @uso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uso.errors.count, "error") %> prohibited this uso from being saved:</h2>

      <ul>
      <% @uso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
