<% if current_user.email == @user.email || current_user.role_id == 1 %>
<center>
	<%= image_tag ("ba.png"), height: '550', width: '550' %>
<div class="registro">
	<h1>Editar usuario</h1>

	<%= render 'form' %>
</div>
</center>
<% else %>
Asceso denegado.

<% end %>